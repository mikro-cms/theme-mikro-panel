import { MomentDirective } from './moment.directive';

describe('MomentDirective', () => {
  it('should create an instance', () => {
    const directive = new MomentDirective();
    expect(directive).toBeTruthy();
  });
});
